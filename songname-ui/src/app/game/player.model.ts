export class Player {
  name: string;
  score: number;
  uuid: string;

  constructor(name: string, score: number, uuid: string) {
    this.name = name;
    this.score = score;
    this.uuid = uuid;
  }

}
